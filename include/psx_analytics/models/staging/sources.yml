version: 2
sources:
  - name: raw
    database: "{{ var('source_database', 'PsxDataLake') }}"
    schema: "{{ var('source_schema', 'public') }}"
    tables:
      - name: PsxAllShr
        description: "Raw PSX daily snapshot data ingested from scraper"
        quoting:
          identifier: true

      - name: psx_price_history
        description: "Raw daily OHLCV price history ingested from the yfinance fetch script"
